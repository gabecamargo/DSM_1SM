const nota1: number = 8.5;
const nota2: number = 7.0;
const nota3: number = 9.2;

const media: number = (nota1 + nota2 + nota3) / 3;

document.write(`<h2>Média Final do Aluno: ${media.toFixed(2)}</h2>`);
